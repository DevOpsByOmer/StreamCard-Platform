apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/DevOpsByOmer/StreamCart-latform.git
    targetRevision: main
    path: gitops/environments/dev/catalog

  destination:
    server: https://kubernetes.default.svc
    namespace: catalog

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
